export type ThirdParty = {
  id: string
  managers: string[]
  name: string
  description: string
  maxItems: string
  totalItems: string
}

export type ThirdPartyItemTier = {
  id: string
  value: string
  price: string
}
